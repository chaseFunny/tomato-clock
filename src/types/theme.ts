export type ThemeType = 'minimal' | 'classic' | 'modern';

export interface ThemeColors {
  background: string;
  card: string;
  text: string;
  primary: string;
  secondary: string;
  accent: string;
}